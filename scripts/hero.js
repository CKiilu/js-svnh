var hero, bad_guy;

function hero() {
    if (hero === "strong") {
        if (bad_guy === "weak") {
            console.log('Ah-ha, an easy victory!');
        }
        console.log('Let us battle to the death!');
    }
}
